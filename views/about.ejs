<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>About | MyExpress</title>

    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;800&display=swap"
      rel="stylesheet"
    />
    <script src="https://unpkg.com/lucide@latest"></script>

    <style>
      :root {
        --primary: #6366f1;
        --bg: #f8fafc;
        --text-main: #0f172a;
        --text-muted: #64748b;
      }

      body {
        font-family: "Plus Jakarta Sans", sans-serif;
        background-color: var(--bg);
        color: var(--text-main);
        padding-top: 50px;
      }

      .container {
        max-width: 900px;
      }

      /* Back Button */
      .btn-back {
        text-decoration: none;
        color: var(--text-muted);
        font-weight: 600;
        display: inline-flex;
        align-items: center;
        gap: 8px;
        margin-bottom: 30px;
        transition: 0.2s;
      }
      .btn-back:hover {
        color: var(--primary);
        transform: translateX(-5px);
      }

      /* Main Content Card */
      .about-card {
        background: #ffffff;
        border-radius: 32px;
        padding: 50px;
        box-shadow: 0 10px 30px -5px rgba(0, 0, 0, 0.04);
        border: 1px solid #f1f5f9;
        text-align: center;
      }

      .badge-premium {
        background: rgba(99, 102, 241, 0.1);
        color: var(--primary);
        padding: 6px 16px;
        border-radius: 100px;
        font-weight: 600;
        font-size: 0.85rem;
        margin-bottom: 20px;
        display: inline-block;
      }

      h1 {
        font-weight: 800;
        font-size: 2.5rem;
        margin-bottom: 20px;
      }

      .lead-text {
        color: var(--text-muted);
        font-size: 1.15rem;
        line-height: 1.7;
        margin-bottom: 40px;
      }

      /* Features Grid */
      .feature-box {
        background: #fdfdff;
        border: 1px solid #f1f5f9;
        border-radius: 20px;
        padding: 25px;
        height: 100%;
        transition: 0.3s;
      }
      .feature-box:hover {
        border-color: var(--primary);
        transform: translateY(-5px);
      }
      .icon-wrapper {
        width: 50px;
        height: 50px;
        background: var(--primary);
        color: white;
        border-radius: 12px;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 15px;
      }

      footer {
        margin-top: 50px;
        color: var(--text-muted);
        font-size: 0.9rem;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <a href="/" class="btn-back">
        <i data-lucide="arrow-left" size="18"></i> Back to Home
      </a>

      <div class="about-card">
        <span class="badge-premium">Version 2.0.0</span>
        <h1>About MyExpress App</h1>
        <p class="lead-text">
          This application is a modern full-stack platform built with
          **Node.js**, **Express**, and **EJS**. It was designed to demonstrate
          efficient student management and product tracking using
          high-performance backend routing.
        </p>

        <div class="row g-4 text-center">
          <div class="col-md-4">
            <div class="feature-box">
              <div class="icon-wrapper">
                <i data-lucide="zap"></i>
              </div>
              <h6>Fast Routing</h6>
              <p class="small text-muted mb-0">
                Powered by Express.js for lightning-quick server responses.
              </p>
            </div>
          </div>
          <div class="col-md-4">
            <div class="feature-box">
              <div class="icon-wrapper">
                <i data-lucide="layout"></i>
              </div>
              <h6>Dynamic UI</h6>
              <p class="small text-muted mb-0">
                EJS templating for seamless server-side rendering.
              </p>
            </div>
          </div>
          <div class="col-md-4">
            <div class="feature-box">
              <div class="icon-wrapper">
                <i data-lucide="database"></i>
              </div>
              <h6>Data Ready</h6>
              <p class="small text-muted mb-0">
                Scalable architecture for student and product databases.
              </p>
            </div>
          </div>
        </div>

        <footer class="pt-4 mt-5 border-top">
          &copy; 2026 MyExpress Project. Built with passion.
        </footer>
      </div>
    </div>

    <script>
      // Initialize icons
      lucide.createIcons();
    </script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
