<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>

    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;600;800&display=swap"
      rel="stylesheet"
    />
    <script src="https://unpkg.com/lucide@latest"></script>

    <style>
      :root {
        --primary: #4f46e5;
        --bg: #f8fafc;
        --card: #ffffff;
        --text-main: #0f172a;
        --text-muted: #64748b;
      }

      body {
        font-family: "Plus Jakarta Sans", sans-serif;
        background: var(--bg);
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        align-items: center;
        padding: 40px 20px;
        margin: 0;
      }

      .nav-header {
        width: 100%;
        max-width: 600px;
        margin-bottom: 24px;
      }

      .btn-back {
        text-decoration: none;
        color: var(--text-muted);
        font-weight: 600;
        display: flex;
        align-items: center;
        gap: 8px;
        transition: 0.2s;
      }

      .btn-back:hover {
        color: var(--primary);
        transform: translateX(-5px);
      }

      .contact-card {
        background: var(--card);
        width: 100%;
        max-width: 600px;
        border-radius: 32px;
        padding: 45px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.04);
        border: 1px solid #f1f5f9;
        animation: fadeIn 0.6s ease-out;
      }

      h1 {
        font-weight: 800;
        font-size: 2rem;
        color: var(--text-main);
        margin-bottom: 10px;
        text-align: center;
      }

      p.subtitle {
        text-align: center;
        color: var(--text-muted);
        margin-bottom: 40px;
      }

      .form-label {
        font-weight: 600;
        color: var(--text-main);
        font-size: 0.9rem;
        margin-bottom: 8px;
      }

      .form-control {
        padding: 12px 16px;
        border-radius: 12px;
        border: 1px solid #e2e8f0;
        background: #fdfdff;
        transition: 0.2s;
      }

      .form-control:focus {
        border-color: var(--primary);
        box-shadow: 0 0 0 4px rgba(79, 70, 229, 0.1);
        outline: none;
      }

      .btn-submit {
        background: var(--primary);
        color: white;
        width: 100%;
        padding: 14px;
        border-radius: 14px;
        font-weight: 600;
        border: none;
        margin-top: 20px;
        transition: 0.3s;
      }

      .btn-submit:hover {
        opacity: 0.9;
        transform: translateY(-2px);
        box-shadow: 0 10px 20px rgba(79, 70, 229, 0.2);
      }

      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(15px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }
    </style>
  </head>
  <body>
    <div class="nav-header">
      <a href="/" class="btn-back">
        <i data-lucide="arrow-left" size="18"></i> Back to Home
      </a>
    </div>

    <div class="contact-card" id="contactContainer">
      <div id="formState">
        <h1>Get in Touch</h1>
        <p class="subtitle">Have a question? We're here to help.</p>

        <form id="contactForm">
          <div class="mb-3">
            <label class="form-label">Full Name</label>
            <input
              type="text"
              id="name"
              class="form-control"
              placeholder="John Doe"
              required
            />
          </div>

          <div class="mb-3">
            <label class="form-label">Email Address</label>
            <input
              type="email"
              id="email"
              class="form-control"
              placeholder="john@example.com"
              required
            />
          </div>

          <div class="mb-4">
            <label class="form-label">Your Message</label>
            <textarea
              id="message"
              class="form-control"
              rows="4"
              placeholder="How can we help you?"
              required
            ></textarea>
          </div>

          <button type="submit" class="btn-submit">Send Message</button>
        </form>
      </div>

      <div id="successState" style="display: none; padding: 40px 0">
        <div
          style="
            background: rgba(34, 197, 94, 0.1);
            color: #15803d;
            width: 64px;
            height: 64px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 20px;
          "
        >
          <i data-lucide="check-circle" size="32"></i>
        </div>
        <h2 style="font-weight: 800">Message Sent!</h2>
        <p class="text-muted">
          Thank you for reaching out. We'll get back to you shortly.
        </p>
        <button
          onclick="location.reload()"
          class="btn btn-outline-secondary mt-4"
          style="border-radius: 12px"
        >
          Send another
        </button>
      </div>
    </div>

    <script>
      // Initialize Lucide Icons
      lucide.createIcons();

      const contactForm = document.getElementById("contactForm");
      const formState = document.getElementById("formState");
      const successState = document.getElementById("successState");

      contactForm.addEventListener("submit", function (e) {
        // 1. Stop the page from reloading/POSTing
        e.preventDefault();

        // 2. Hide the form with a fade effect
        formState.style.transition = "opacity 0.4s ease";
        formState.style.opacity = "0";

        setTimeout(() => {
          formState.style.display = "none";

          // 3. Show success message
          successState.style.display = "block";
          successState.style.opacity = "0";
          setTimeout(() => {
            successState.style.transition = "opacity 0.4s ease";
            successState.style.opacity = "1";
            lucide.createIcons(); // Refresh icons for the success view
          }, 50);
        }, 400);
      });
    </script>
  </body>
</html>
