<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>

    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      }

      body {
        height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        background: linear-gradient(135deg, #667eea, #764ba2);
        color: #333;
      }

      .card {
        background: #ffffff;
        padding: 35px 40px;
        border-radius: 14px;
        text-align: center;
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.2);
        max-width: 400px;
        width: 100%;
        animation: fadeIn 0.8s ease-in-out;
      }

      h1 {
        font-size: 26px;
        margin-bottom: 10px;
        color: #2d2d2d;
      }

      p {
        font-size: 15px;
        color: #555;
      }

      .highlight {
        color: #667eea;
        font-weight: 600;
      }

      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(15px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }
    </style>
  </head>

  <body>
    <div class="card">
      <h1>
        Welcome,
        <span class="highlight"><%= name %></span> 👋
      </h1>
      <p>This page is dynamically rendered using the EJS template engine.</p>
    </div>
  </body>
</html>
